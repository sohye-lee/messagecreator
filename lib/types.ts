export interface MessageType {
  category: string;
  list: string[];
}

export interface MessageInfo {
  purpose: string;
  occasion: string;
  relation: string;
  tone: string;
  length: string;
  urgency: string;
  theme: string;
  details: string;
}
